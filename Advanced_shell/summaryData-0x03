#!/bin/bash

DATA_DIR="pokemon_data"
CSV_FILE="pokemon_report.csv"

# Create CSV header
echo "Name,Height (m),Weight (kg)" > "$CSV_FILE"

# Loop through JSON files
for file in "$DATA_DIR"/*.json; do
    name=$(jq -r '.name' "$file" | sed 's/.*/\u&/')  # Capitalize first letter using sed
    height_dm=$(jq -r '.height' "$file")
    weight_hg=$(jq -r '.weight' "$file")

    # Convert units
    height=$(awk "BEGIN {printf \"%.1f\", $height_dm / 10}")
    weight=$(awk "BEGIN {printf \"%.1f\", $weight_hg / 10}")

    # Append to CSV
    echo "$name,$height,$weight" >> "$CSV_FILE"
done

# Calculate averages with awk
avg_height=$(awk -F, 'NR>1 {sum+=$2} END {printf "%.2f", sum/(NR-1)}' "$CSV_FILE")
avg_weight=$(awk -F, 'NR>1 {sum+=$3} END {printf "%.2f", sum/(NR-1)}' "$CSV_FILE")

# Print report
echo "CSV Report generated at: $CSV_FILE"
cat "$CSV_FILE"
echo ""
echo "Average Height: $avg_height m"
echo "Average Weight: $avg_weight kg"
